# Base image for frontend build
# Pre-installs all node_modules for offline deployment

FROM node:18-alpine

WORKDIR /app

# Copy package files
COPY frontend/package*.json ./

# Install all dependencies
RUN npm install --legacy-peer-deps

# This base image has all node_modules installed
# The actual build will just copy source and run build
