# Offline-ready Dockerfile for admin-service
# Uses pre-built base image with all dependencies installed
# No network access required during build

FROM nap-python-base:latest

WORKDIR /app

# Copy shared libraries
COPY shared /app/shared

# Copy service code
COPY services/admin-service/app /app

# Expose port
EXPOSE 3005

# Run the service
CMD ["python", "main.py"]
